apiVersion: v1
kind: Service
metadata:
  name: hello-world-svc
  namespace: cloudnautic
  labels:
    app: hello-world
spec:
  type: LoadBalancer
  selector:
    app: hello-world
  ports:
    - name: http
      port: 80
      targetPort: 80
